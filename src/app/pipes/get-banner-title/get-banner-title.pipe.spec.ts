import { GetBannerTitlePipe } from './get-banner-title.pipe';

describe('GetBannerTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetBannerTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
