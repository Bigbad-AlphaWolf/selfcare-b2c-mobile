import { SearchAssistancePipe } from './search-assistance.pipe';

describe('SearchAssistancePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAssistancePipe();
    expect(pipe).toBeTruthy();
  });
});
