import { GetBannerDescriptionPipe } from './get-banner-description.pipe';

describe('GetBannerDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetBannerDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
