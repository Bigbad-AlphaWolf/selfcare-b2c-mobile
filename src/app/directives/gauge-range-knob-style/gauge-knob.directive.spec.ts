import { GaugeKnobDirective } from './gauge-knob.directive';

describe('GaugeKnobDirective', () => {
  it('should create an instance', () => {
    const directive = new GaugeKnobDirective();
    expect(directive).toBeTruthy();
  });
});
