import { OemStatusSargalLabelDirective } from './oem-status-sargal-label.directive';

describe('OemStatusSargalLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new OemStatusSargalLabelDirective();
    expect(directive).toBeTruthy();
  });
});
