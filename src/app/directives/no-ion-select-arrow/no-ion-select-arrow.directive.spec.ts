import { NoIonSelectArrowDirective } from './no-ion-select-arrow.directive';

describe('NoIonSelectArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new NoIonSelectArrowDirective();
    expect(directive).toBeTruthy();
  });
});
