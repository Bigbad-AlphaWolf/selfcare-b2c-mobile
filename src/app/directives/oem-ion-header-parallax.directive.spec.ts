import { OemIonHeaderParallaxDirective } from './oem-ion-header-parallax.directive';

describe('OemIonHeaderParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new OemIonHeaderParallaxDirective();
    expect(directive).toBeTruthy();
  });
});
