import { HeaderScrollEffectDirective } from './header-scroll-effect.directive';

describe('HeaderScrollEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderScrollEffectDirective();
    expect(directive).toBeTruthy();
  });
});
