import { StatusBillPaymentPipe } from './status-bill-payment.pipe';

describe('StatusBillPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusBillPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
