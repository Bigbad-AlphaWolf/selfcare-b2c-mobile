import { DisplayFileManagerImagePipe } from './display-file-manager-image.pipe';

describe('DisplayFileManagerImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFileManagerImagePipe();
    expect(pipe).toBeTruthy();
  });
});
