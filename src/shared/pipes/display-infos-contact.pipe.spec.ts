import { DisplayInfosContactPipe } from './display-infos-contact.pipe';

describe('DisplayInfosContactPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayInfosContactPipe();
    expect(pipe).toBeTruthy();
  });
});
