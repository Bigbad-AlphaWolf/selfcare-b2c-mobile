import { IlliflexVoicePipe } from './illiflex-voice.pipe';

describe('IlliflexVoicePipe', () => {
  it('create an instance', () => {
    const pipe = new IlliflexVoicePipe();
    expect(pipe).toBeTruthy();
  });
});
