import { PublicationDateFormatPipe } from './publication-date-format.pipe';

describe('PublicationDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PublicationDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
