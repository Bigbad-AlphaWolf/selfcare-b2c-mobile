import { PassVolumeDisplayPipe } from './pass-volume-display.pipe';

describe('PassVolumeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PassVolumeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
