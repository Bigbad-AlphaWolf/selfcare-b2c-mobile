import { GenerateIdForTAPipe } from './generate-id-for-ta.pipe';

describe('GenerateIdForTAPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateIdForTAPipe();
    expect(pipe).toBeTruthy();
  });
});
