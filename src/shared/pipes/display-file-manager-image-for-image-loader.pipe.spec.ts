import { DisplayFileManagerImageForImageLoaderPipe } from './display-file-manager-image-for-image-loader.pipe';

describe('DisplayFileManagerImageForImageLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFileManagerImageForImageLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
