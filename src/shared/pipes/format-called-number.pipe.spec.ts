import { FormatCalledNumberPipe } from './format-called-number.pipe';

describe('FormatCalledNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCalledNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
