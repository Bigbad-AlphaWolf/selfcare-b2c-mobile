import { FormatDataVolume2Pipe } from './format-data-volume-2.pipe';

describe('FormatDataVolume2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDataVolume2Pipe();
    expect(pipe).toBeTruthy();
  });
});
