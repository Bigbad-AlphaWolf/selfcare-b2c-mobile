import { ConsoNameDisplayPipe } from './conso-name-display.pipe';

describe('ConsoNameDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsoNameDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
