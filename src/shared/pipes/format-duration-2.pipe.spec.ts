import { FormatDuration2Pipe } from './format-duration-2.pipe';

describe('FormatDuration2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDuration2Pipe();
    expect(pipe).toBeTruthy();
  });
});
