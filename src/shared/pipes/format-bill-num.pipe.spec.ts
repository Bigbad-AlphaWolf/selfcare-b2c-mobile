import { FormatBillNumPipe } from './format-bill-num.pipe';

describe('FormatBillNumPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBillNumPipe();
    expect(pipe).toBeTruthy();
  });
});
