import { FormatBillDatePipe } from './format-bill-date.pipe';

describe('FormatBillDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBillDatePipe();
    expect(pipe).toBeTruthy();
  });
});
