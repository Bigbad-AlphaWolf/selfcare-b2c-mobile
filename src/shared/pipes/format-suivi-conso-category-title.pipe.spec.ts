import { FormatSuiviConsoCategoryTitlePipe } from './format-suivi-conso-category-title.pipe';

describe('FormatSuiviConsoCategoryTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSuiviConsoCategoryTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
